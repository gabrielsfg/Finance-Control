namespace FinanceControl.Shared.Enums
{
    /// Derived from the questionnaire by a fixed rule in RiskProfileService — never
    /// inferred from behaviour, and never crossed with a specific asset.
    public enum EnumRiskClassification
    {
        Conservative,
        Moderate,
        Aggressive
    }
}
