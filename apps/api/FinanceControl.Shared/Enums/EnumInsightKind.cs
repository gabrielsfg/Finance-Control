namespace FinanceControl.Shared.Enums
{
    /// The two analyses in scope. Both are descriptive and retrospective; there is no
    /// kind that recommends anything, by design.
    public enum EnumInsightKind
    {
        SpendingWeekly,
        PortfolioSnapshot
    }
}
